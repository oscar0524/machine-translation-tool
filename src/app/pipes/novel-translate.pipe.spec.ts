import { NovelTranslatePipe } from './novel-translate.pipe';

describe('NovelTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new NovelTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
